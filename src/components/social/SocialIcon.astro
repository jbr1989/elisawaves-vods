---
const { title, url, icon } = Astro.props;
---

<a href={url} target="_blank" rel="noopener noreferrer" aria-label={`Visitar ${title}`}>
  <img src={icon} alt={`Logo de ${title}`} class="w-9 h-9 inline-block" />
</a>
