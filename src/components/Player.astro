---
import YouTubeFacade from "./YouTubeFacade.astro";

interface Props {
  playlistId?: string;
  videoId: string;
  title?: string;
}

const { playlistId, videoId, title } = Astro.props;
---

<YouTubeFacade playlistId={playlistId} videoId={videoId} title={title} />
